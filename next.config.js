const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      "api.bdsconnect.in",
      "bds-files-uploads.s3.ap-south-1.amazonaws.com",
      "localhost",
    ],
  },
};

module.exports = nextConfig;
